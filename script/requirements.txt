# Phase 3 export script dependencies. Pinned for reproducibility.
# Use `python3 -m venv .venv && source .venv/bin/activate && pip install -r script/requirements.txt`.
# torch version is matched to the libtorch shipped in homebrew's `pytorch`
# formula (and to torch-rb 0.24.0's expected ABI).
torch==2.12.0
numpy>=1.26,<2.0       # nnunetv2 transitively requires numpy<2
pyyaml>=6.0
totalsegmentator==2.4.0
nnunetv2==2.5.1
